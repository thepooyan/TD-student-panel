mixin summary(id, title, sample='نمونه')
    .summary(id= id)
        h3
            .crumb 
                .in
            =`${title}`
        .items 
            .item= sample
            .item= sample
            .item= sample
            .item= sample
        button بستن



mixin comment(user, text, type=false)
    div(class=type?'others':'')
        .icon(data-user=user)
        span
            =text